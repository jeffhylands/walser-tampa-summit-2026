# 2026 Tampa Bay Retirement Summit — landing page

Preview build for internal review. Not launched.

- `index.html` — the lander (single file, styles and script inline)
- `Brand Assets/` — logo, Charles Payne headshot, Rebecca headshot, Tampa
  skyline plate, news reel + its poster frame, award badges

## Before launch
1. Delete the `.draft` banner block in `index.html` and its dismiss handler.
2. Remove the two `noindex` meta tags in `<head>` and `robots.txt`.
3. Work the `SWAP LIST` at the top of `index.html` — prices, date, venue,
   times, checkout URLs, event photography.
4. Re-cut the hero news reel to 30-60s; 46 MB is heavy for a hero.

Source of truth for edits is `tampa-summit.html` in the parent folder.
